before_script:
  - apt-get update -qq && apt-get install -y -qq python3-dev python3-pip
  - ls /usr/bin/python*
  - python3 -v
test_py34:
  script:
    - pip3 install tox
    - export TOXENV=py34
    - tox
